version: "2"

run:
  timeout: 5m
  build-tags:
    - fts5

linters:
  enable:
    - asciicheck
    - bodyclose
    - copyloopvar
    - dogsled
    - durationcheck
    - errcheck
    - errorlint
    - exhaustive
    - gocyclo
    - gocritic
    - goprintffuncname
    - gosec
    - govet
    - ineffassign
    - misspell
    - modernize
    - nakedret
    - nilerr
    - prealloc
    - revive
    - staticcheck
    - unconvert
    - unparam
    - unused
    - whitespace
  settings:
    errcheck:
      exclude-functions:
        - io.Copy
        - io/ioutil.ReadFile
        - io/ioutil.WriteFile
    errorlint:
      errorf: false
      asserts: false
      comparison: true
    gocritic:
      disabled-checks:
        - ifElseChain
    gosec:
      includes:
        - G104
        - G108
        - G109
        - G112
        - G114
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    rules:
      - linters:
          - revive
        text: if-return

formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    generated: lax
